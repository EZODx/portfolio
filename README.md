# Portfolio

A personal repository documenting my programming learning journey — problems, projects, and implementations organized by batch.

---

## Structure

```
portfolio/
├── batch-1/
├── batch-2/
├── batch-3/
└── batch-4/
```

Each batch contains a `problems/` folder, a `projects/` folder (where applicable), and a `README.md`.